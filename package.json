{
  "scripts": {
    "cy:open": "cypress open",
    "test": "cypress run --reporter nyan --browser chrome & npm run reporter",
    "reporter": "node cypress/support/report-generate-formatter/report.js"
  },
  "devDependencies": {
    "cucumber-html-reporter": "^6.0.0",
    "cypress": "^12.4.0",
    "cypress-cucumber-preprocessor": "^4.3.1",
    "cypress-xpath": "^2.0.1",
    "multiple-cucumber-html-reporter": "^3.6.1"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": false,
    "cucumberJson": {
      "generate": true,
      "outputFolder": "cypress/reports/cucumber-json",
      "filePrefix": "",
      "fileSuffix": ".cucumber"
    },
    "stepDefinitions": "cypress/support/steps"
  }
}